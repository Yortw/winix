{
  "schema_version": 1,
  "tool": "mksecret key",
  "version": "<normalised>",
  "maturity": "fresh",
  "description": "Generate an encoded high-entropy key (API key, OAuth secret, HMAC key) from random bytes.",
  "platform": {
    "scope": "cross-platform",
    "replaces": [
      "pwgen",
      "openssl rand",
      "diceware/xkcdpass",
      "PowerShell Get-SecureRandom"
    ],
    "value_on_windows": "Windows ships no secure generator out of the box; the common Get-Random idiom is non-cryptographic. mksecret is secure-by-default with no PowerShell-version dependency.",
    "value_on_unix": "One self-contained binary covering passwords, diceware passphrases (missing on every OS), and encoded keys — secure-by-default (no pwgen -s footgun, no Python runtime for diceware)."
  },
  "prefer_default_when": [
    "hashing or signing data — use digest",
    "unique identifiers (UUID/ULID/NanoID) — use ids"
  ],
  "usage": "mksecret key [options]",
  "options": [
    {
      "long": "--help",
      "short": "-h",
      "type": "flag",
      "description": "Show help",
      "repeatable": false
    },
    {
      "long": "--version",
      "type": "flag",
      "description": "Show version",
      "repeatable": false
    },
    {
      "long": "--no-color",
      "type": "flag",
      "description": "Disable colored output",
      "repeatable": false
    },
    {
      "long": "--json",
      "type": "flag",
      "description": "JSON output",
      "repeatable": false
    },
    {
      "long": "--describe",
      "type": "flag",
      "description": "Structured JSON metadata for AI agents",
      "repeatable": false
    },
    {
      "long": "--quiet",
      "type": "flag",
      "description": "Suppress the stderr entropy note.",
      "repeatable": false
    },
    {
      "long": "--count",
      "short": "-n",
      "type": "int",
      "placeholder": "N",
      "description": "Number of secrets to emit, one per line. Default 1 (max 100000).",
      "repeatable": false
    },
    {
      "long": "--bytes",
      "short": "-b",
      "type": "int",
      "placeholder": "N",
      "description": "Number of random bytes. Default 32 (256-bit, max 65536).",
      "repeatable": false
    },
    {
      "long": "--encoding",
      "short": "-e",
      "type": "string",
      "placeholder": "NAME",
      "description": "Encoding: base64url (default, unpadded), base64, hex, base32.",
      "repeatable": false
    },
    {
      "long": "--color",
      "type": "optional-value",
      "allowed_values": [
        "auto",
        "always",
        "never"
      ],
      "default_when_bare": "always",
      "description": "Coloured output: auto (default when omitted), always, or never",
      "repeatable": false
    }
  ],
  "exit_codes": [
    {
      "code": 0,
      "description": "Success (a closed downstream pipe, e.g. | head -1, also exits 0 — not an error)"
    },
    {
      "code": 125,
      "description": "Usage error: unknown flag, bad --charset/--encoding value, non-positive or oversized --length/--bytes/--words/--count, unexpected positional (including an unrecognised subcommand)"
    },
    {
      "code": 126,
      "description": "Runtime error: OS CSPRNG failure or output write failure (disk full, device error)"
    }
  ],
  "io": {
    "stdin": "Not used.",
    "stdout": "The generated secret(s), one per line; or a JSON envelope under --json.",
    "stderr": "Entropy note (≈ N bits) unless --quiet/--json; errors."
  },
  "examples": [
    {
      "command": "mksecret key",
      "description": "32 random bytes as unpadded base64url"
    },
    {
      "command": "mksecret key --bytes 64 --encoding hex",
      "description": "64 bytes as hex"
    },
    {
      "command": "mksecret key --encoding base32",
      "description": "Crockford base32 (ambiguity-free)"
    }
  ]
}