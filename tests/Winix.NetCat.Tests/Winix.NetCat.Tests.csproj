<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <IsTestProject>true</IsTestProject>
    <!-- Mirror the production csproj so framework ex.Message returns bare CoreLib resource
         keys (UseSystemResourceKeys=true) in tests too — otherwise SR-key-leak regressions
         are invisible locally. Part of the suite-wide resource-key-leak sweep. -->
    <UseSystemResourceKeys>true</UseSystemResourceKeys>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="18.*" />
    <PackageReference Include="xunit" Version="2.*" />
    <PackageReference Include="xunit.runner.visualstudio" Version="3.*" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\..\src\Winix.NetCat\Winix.NetCat.csproj" />
    <!-- Force-build the console app so ProgramMainTests can spawn nc.dll. -->
    <ProjectReference Include="..\..\src\nc\nc.csproj" ReferenceOutputAssembly="false" OutputItemType="None" />
  </ItemGroup>
</Project>
